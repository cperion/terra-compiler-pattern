local U = require("unit")

return function(T, U, P)
    local tests = {}

    function tests.test_lower()
        error("scaffold: fill in test for FrontendChecked.Spec:lower", 2)
        -- local input = T.FrontendChecked.Spec(...)
        -- local out = input:lower()
        -- assert(out ~= nil)
    end

    return tests
end
