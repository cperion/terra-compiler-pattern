local U = require("unit")

return function(T, U, P)
    local tests = {}

    function tests.test_define_machine()
        error("scaffold: fill in test for FrontendLowered.Spec:define_machine", 2)
        -- local input = T.FrontendLowered.Spec(...)
        -- local out = input:define_machine()
        -- assert(out ~= nil)
    end

    return tests
end
