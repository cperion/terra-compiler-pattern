return function(T, U, P)
    -- MoreFunLuaJIT.Spec was removed so the terminal receiver can be the real
    -- machine-near noun: MoreFunLuaJIT.Plan.
    --
    -- This file remains as a harmless no-op because project boundary loading is
    -- file-based and older scaffold files may still exist in working trees.
end
